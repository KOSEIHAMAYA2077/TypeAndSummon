# 共同開発の進め方

このリポジトリはグループ課題用です。作業が重ならないよう、変更ごとにブランチを分けます。

## 基本手順

1. 最新の `main` を取得する
2. `feature/変更内容` という名前でブランチを作る
3. 変更して動作確認する
4. 変更内容が分かる短いメッセージでコミットする
5. GitHubへpushし、Pull Requestを作る
6. メンバーに確認してもらってから `main` へマージする

## 登録しないファイル

- `server/.env` と `client/.env`
- コンパイルで作られる `.class` ファイル
- 実行中に作られる `server/data/game_server.db`
- IDEやOSが自動生成する設定ファイル

環境設定が必要な場合は、各ディレクトリの `.env.example` をコピーして使用してください。

